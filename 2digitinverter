/* Inverts a two digit number*/ 

#include <stdio.h>

int main(void)
{
    int n1, n2, n3;
    
    printf ("Please enter a two digit number: ");
    scanf ("%d", &n1);
    
    n2 = n1 % 10;
    n3 = n1 / 10;
    
    printf ("The inverted number is : %d%d", n2, n3);
    
    return 0;
}
