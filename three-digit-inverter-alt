/* Inverts a three digit number*/ 

#include <stdio.h>

int main(void)
{
    int n1, n2, n3;
    
    printf ("Please enter a three digit number: ");
    scanf ("%1d%1d%1d", &n3, &n2, &n1);
    

    printf ("The inverted number is : %d%d%d", n1, n2, n3);
    
    return 0;
}
